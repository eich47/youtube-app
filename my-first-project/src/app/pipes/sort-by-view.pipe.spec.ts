import { SortByViewPipe } from './sort-by-view.pipe';

describe('SortByViewPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByViewPipe();
    expect(pipe).toBeTruthy();
  });
});
